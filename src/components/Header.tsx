import { FC } from "react";

export const Header: FC = () => {
    return (
        <div>
            <div>
                <img src="img/waves.svg" alt="Waves" />
            </div>

            <h1>Cluster.js</h1>
        </div>
    );
};
